
SET SERVEROUTPUT ON;
CREATE OR REPLACE PROCEDURE DivideProblem(a in NUMBER,b in NUMBER)
IS
	c NUMBER;
BEGIN
	C:=a/b;
	DBMS_OUTPUT.PUT_LINE(C);
EXCEPTION
	WHEN Zero_Divide THEN
		DBMS_OUTPUT.PUT_LINE("CANNOT DIVIDE BY ZERO");
END;
/


BEGIN
	DivideProblem(2,0);
END;
/